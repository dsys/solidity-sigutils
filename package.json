{
  "name": "solidity-signatureutils",
  "version": "1.0.0",
  "description": "Solidity library for (multi)signatures",
  "main": "index.js",
  "repository": "git@github.com:cleargraphinc/solidity-signatureutils.git",
  "author": "alex@cleargraph.com",
  "license": "Apache-2",
  "scripts": {
    "start": "truffle develop",
    "test": "truffle test",
    "watch": "watchman-make -p 'contracts/**/*.sol' 'test/**/*.sol' --run 'yarn test'",
    "compile": "truffle compile",
    "migrate": "truffle migrate --reset --compile-all",
    "migrate-rinkeby": "truffle migrate --reset --compile-all --network rinkeby",
    "migrate-mainnet": "truffle migrate --compile-all --network mainnet",
    "lint": "solium -d contracts/",
    "fix": "solium --fix -d contracts/"
  },
  "devDependencies": {
    "truffle": "^4.1.11",
    "eth-gas-reporter": "^0.1.2",
    "solium": "^1.1.7",
    "truffle-privatekey-provider": "^0.0.6"
  }
}
